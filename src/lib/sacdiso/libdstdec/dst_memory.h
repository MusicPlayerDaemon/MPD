#ifndef _DST_MEMORY_H_INCLUDED
#define _DST_MEMORY_H_INCLUDED

#include <cstring>

#define dst_memcpy(dst, src, size) memcpy(dst, src, size)
#define dst_memset(dst, val, size) memset(dst, val, size)

#endif
